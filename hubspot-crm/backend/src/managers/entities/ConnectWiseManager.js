const { ModuleManager } = require('@friggframework/api-module-connectwise');

class ConnectWiseManager extends ModuleManager {
    constructor(params) {
        return super(params);
    }
}

module.exports = ConnectWiseManager;
